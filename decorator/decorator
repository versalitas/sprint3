const conversionObj = requiere('./currency_conversion.json');

function getPriceEuro(target) {
    target.prototype.conversion() = 
}


class Article {
    
    constructor({name, price, currency}){
    this.name = obj.name;
    this.price = obj.price;
    this.currency = obj.currency;
    }
    @getPriceEuro
    getPrice() {
        return this.price;
    }
}

    }

    getPrice(){
        return this.article.getPrice();
    }   

}

//concrete decorator adding the currency converting feature
class ArticlePriceInEUDecorator extends ArticleDecorator{
    getPrice(){
        return 
    }
}
*/